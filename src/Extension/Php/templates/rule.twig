{% set result_var = '$result_' ~ expr.id %}
/**
 * Matching rule: {{ expr.name }} <- {{ expr }}
 */
public function match_{{ expr.name }}()
{
    {{ result_var }} = null;
{% filter indent -%}
{% include expr_tpl(expr) with {expr: expr} only %}
{%- endfilter %}
    return {{ result_var }};
}
