{% set subres_var = result_varname(expr[0]) %}
// Label: {{ expr }}
{{ subres_var }} = null;
{{ render_expr(expr[0]) }}
// Label: {{ expr }} => Storing result in scope
if ({{ subres_var }}) {
    $scope['{{ expr.label }}'] = substr($this->text, {{ subres_var }}->start, {{ subres_var }}->end - {{ subres_var }}->start);
}
